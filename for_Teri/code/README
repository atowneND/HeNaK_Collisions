code:
    plotAll:
        - runs B_lambda and B_alpha analysis
        - if the target_directory and appropriate subdirectories do not exist, it will create them
        USAGE: ./plotAll <target_directory> <blam_file>
        -> target_directory
            -> Blambdas
                -> Blambda_##_##.dat (j, j')
                    - output file of dqcalc
            -> Blams
                -> Blam_##_##.dat (j,j')
                    - normalized B-lambdas, from dq2theta
            -> Bthetas
                -> Btheta_##_##_dj##.dat (j,j',delta-j)
                    - output file of dq2theta
            -> dtheta
                -> dtheta_dj##.dat (delta-j)
                    - table of delta-thetas for a particular delta-j
            -> plots
                -> BPlots_##_##_dj##.ps (j,j',delta-j)
                    - plots created from <plotme>
            -> Stats
                -> stats_dj##.dat (delta-j)
                    - average, standard deviations, etc.
                    - there are other files to be added
        - STDOUT: prints atom and delta-j currently running (status printout)
    getdata:
        - creates useful data files from raw blam file for cross section analysis
        - runs <calcmm> looping over j, j', and delta-j values
        USAGE: ./getdata <atom> <blam_file>
        INPUTS:
            - atom: He or Ar - used for naming conventions
            - blam_file: blam file path - if relative path breaks it, try using the absolute path
        OUTPUTS:
            -> <./datadir/jmp.dat> useful datafile for j,j' transition
    dmscript:
        USAGE: ./dmscript

->: creates file
